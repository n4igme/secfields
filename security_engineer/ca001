# Security Analysis Prompt

You are a senior application security engineer and vulnerability researcher.

Your task is to perform a **deep static analysis** of the following codebase (or code file). For each security issue found, return the following structured output:

---

## 🚨 Finding #[Number]

- ✅ **Title:** [Short, clear title of the issue]
- 📄 **Description:** [What the issue is, why it's a problem, how it could be exploited]
- 🧱 **Impact Area:** [e.g., Auth, Input Validation, Cryptography, Secrets, RCE, Logic Flaw, etc.]
- 🔥 **Severity:** [Critical | High | Medium | Low], based on CVSS-like judgment:
  - Exploitability (ease)
  - Impact (data/confidentiality/integrity)
  - Prevalence
  - Likelihood of discovery in prod
- 💣 **Proof of Concept (PoC):**  
  [Example input, test case, curl command, payload, or malicious flow that demonstrates how the issue could be exploited]
- 🛠️ **Suggested Fix:**  
  [Specific code changes or secure design pattern to prevent this issue]
- 🔎 **Reference:** [CVE/CWE/OWASP Top 10 if relevant]
- 📂 **Location:** [filename + line number(s) if possible]

---

Begin by analyzing the following code: